# Control how mixture components are combined

Control how mixture components are combined

## Usage

``` r
set_mixture_options(spec, mode = "sample", reference = NULL)
```

## Arguments

- spec:

  A \`race_spec\` object.

- mode:

  Mixture mode.

- reference:

  Optional reference component.

## Value

The updated \`race_spec\`.
