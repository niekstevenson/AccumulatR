% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood_param_interface.R
\name{make_context}
\alias{make_context}
\title{Build a compiled likelihood context from a model}
\usage{
make_context(structure, prep = NULL, native_bundle = NULL)
}
\arguments{
\item{structure}{Finalized model structure.}

\item{prep}{Optional preprocessed model bundle.}

\item{native_bundle}{Optional serialized native bundle.}
}
\value{
An `accumulatr_context` object.
}
\description{
A context stores compiled model/runtime state only. Behavioral data are
prepared separately with `prepare_data()` and supplied to
`log_likelihood()`.
}
\examples{
spec <- race_spec()
spec <- add_accumulator(spec, "A", "lognormal")
spec <- add_outcome(spec, "A_win", "A")
structure <- finalize_model(spec)
make_context(structure)
}
