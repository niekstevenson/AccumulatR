% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood_param_interface.R
\name{log_likelihood}
\alias{log_likelihood}
\alias{log_likelihood.likelihood_context}
\alias{log_likelihood.default}
\title{Native log-likelihood evaluation over multiple parameter sets}
\usage{
log_likelihood(likelihood_context, parameters, ...)

\method{log_likelihood}{likelihood_context}(likelihood_context, parameters, ...)

\method{log_likelihood}{default}(likelihood_context, ...)
}
\arguments{
\item{likelihood_context}{Context built by build_likelihood_context}

\item{parameters}{Parameter data frame, or list of parameter data frames}

\item{...}{Unused; for S3 compatibility}
}
\value{
Numeric vector of log-likelihood values
}
\description{
Native log-likelihood evaluation over multiple parameter sets
}
\examples{
spec <- race_spec()
spec <- add_accumulator(spec, "A", "lognormal",
  params = list(meanlog = 0, sdlog = 0.1))
spec <- add_outcome(spec, "A_win", "A")
structure <- finalize_model(spec)
params_df <- build_params_df(
  spec,
  c(A.meanlog = 0, A.sdlog = 0.1, A.q = 0, A.t0 = 0),
  n_trials = 2
)
data_df <- simulate(structure, params_df, seed = 1)
ctx <- build_likelihood_context(structure, params_df, data_df)
log_likelihood(ctx, list(params_df))
}
