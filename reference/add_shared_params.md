# Tie parameters across accumulators

Tie parameters across accumulators

## Usage

``` r
add_shared_params(spec, members, ...)
```

## Arguments

- spec:

  A \`race_spec\` object.

- members:

  Accumulator labels that should share parameter values.

- ...:

  Named parameters to share.

## Value

The updated \`race_spec\`.
