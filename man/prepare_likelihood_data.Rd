% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood_param_interface.R
\name{prepare_likelihood_data}
\alias{prepare_likelihood_data}
\title{Precompute compact likelihood inputs for repeated evaluations}
\usage{
prepare_likelihood_data(likelihood_context, data)
}
\arguments{
\item{likelihood_context}{Context built by build_likelihood_context}

\item{data}{Data frame with observed outcomes/RTs}
}
\value{
Prepared likelihood inputs suitable for reuse in log_likelihood
}
\description{
Precompute compact likelihood inputs for repeated evaluations
}
\examples{
spec <- race_spec()
spec <- add_accumulator(spec, "A", "lognormal")
spec <- add_outcome(spec, "A_win", "A")
structure <- finalize_model(spec)
params_df <- build_param_matrix(
  spec,
  c(A.meanlog = 0, A.sdlog = 0.1, A.q = 0, A.t0 = 0),
  n_trials = 2
)
data_df <- simulate(structure, params_df, seed = 1)
ctx <- build_likelihood_context(structure, data_df)
prep_data <- prepare_likelihood_data(ctx, data_df)
log_likelihood(ctx, prep_data, list(params_df))
}
