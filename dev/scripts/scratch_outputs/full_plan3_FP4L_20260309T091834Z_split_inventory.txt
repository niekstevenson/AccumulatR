FP4L.4 planner convergence state after keepable cut
1. TrialContributionSpec now carries one per-step payload shape for sequence-density contributions:
   - step competitor payloads
   - step persistent-source payloads
   - step keep-label payloads
   - step guess-shortcut payloads
2. Ranked path no longer rebuilds competitor/persistent-source metadata inside sequence_prefix_density_resolved.
3. Observed single-step still executes outside sequence-state machinery but reads the same per-step payload shape.
4. Remaining Step 4 split points:
   - ranked-only sequence-state scheduling/state collapse
   - single-step guess shortcut specialization
   - closure-level deletion proof for duplicate lower-layer helper reachability
